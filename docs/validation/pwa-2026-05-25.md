# PWA Validation Evidence

Date: 2026-05-25

Status: evidence template created; production audit not yet run.

## Static Checks

- [ ] `apps/web/public/manifest.json` parses as JSON.
- [ ] Manifest icons exist under `apps/web/public/icons/`.
- [ ] `apps/web/public/sw.js` bypasses non-GET requests.
- [ ] `apps/web/public/sw.js` bypasses `/api/**` and protected app routes.
- [ ] `apps/web/src/app/offline/page.tsx` renders without client fetches.

## Production Build

Command:

```bash
pnpm --filter @topshelf/web build
```

Result:

- [ ] Pass
- [ ] Fail

Notes:

## Lighthouse Mobile PWA Audit

Environment:

- URL:
- Browser/device profile:
- Date/time:

Results:

- PWA score:
- Installable:
- Offline fallback:
- Manifest warnings:
- Service worker warnings:

## Real-Device Install/Offline Check

Device:

Steps:

1. Open deployed URL.
2. Confirm install prompt or add-to-home-screen path.
3. Install app.
4. Launch from home screen.
5. Enable airplane mode.
6. Reload app shell.
7. Confirm protected actions do not imply unsaved offline writes.

Result:

- [ ] Pass
- [ ] Fail

Notes:
