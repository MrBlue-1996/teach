# PWA Validation Evidence

Date: 2026-05-26
Validator: Codex
URL: local production build, `http://localhost:3001/kitchen`

## Static Checks

- Manifest parse: passed (`manifest ok`).
- Icons present: passed (`icons ok` for icon-192, icon-512, maskable-192, maskable-512, and apple touch icon).
- Service worker route/cache review: `sw.js` uses versioned caches, bypasses non-GET/API/private routes, precaches install icons, and falls back to `/offline` for failed navigations.
- Service worker upgrade story: `skipWaiting()` is intentionally limited to the v0.1.x static, read-only app shell with versioned cache cleanup and no offline writes.
- Layout metadata: manifest is linked through Next metadata; viewport uses `themeColor: "#050507"` and allows normal scaling.
- Manifest values: `name` = `Top Shelf Teaching`, `short_name` = `Top Shelf`, `start_url` = `/kitchen`, `display` = `standalone`, theme/background = `#050507`, includes any-purpose and maskable icons.
- Offline page: static server component review passed; no fetch or client runtime dependency.

## Production Build And Start

Command:

```bash
pnpm --filter @topshelf/web build
PORT=3001 pnpm --filter @topshelf/web start
```

Result: passed.

## Lighthouse Mobile Audit

Command:

```bash
pnpm dlx lighthouse@12 http://localhost:3001/kitchen \
  --form-factor=mobile \
  --screenEmulation.mobile=true \
  --screenEmulation.width=390 \
  --screenEmulation.height=844 \
  --screenEmulation.deviceScaleFactor=3 \
  --only-categories=performance,accessibility,best-practices \
  --chrome-flags="--headless=new --no-sandbox" \
  --output=json \
  --output=html \
  --output-path=./tmp/lighthouse-pwa-kitchen
```

Artifacts: Lighthouse JSON/HTML were generated locally during validation and summarized below; raw scratch files were not retained in the commit.

Scores:

- Performance: 99.
- Accessibility: 100.
- Best Practices: 100.
- PWA: not available as a Lighthouse category in the local Lighthouse 12/13 CLI used here, so install/offline behavior was validated with manifest/source checks plus a Playwright service-worker smoke.

Notable passing audits: contrast, viewport scaling, skip link, touch targets, console errors, HTTPS/localhost, and viewport metadata.

Installability: manifest and icon checks passed locally. Real install prompt not exercised without a reachable HTTPS device URL or tunnel.

## Offline Behavior

Steps:

1. Load `/kitchen` from the local production server.
2. Confirm the service worker installs.
3. Disable network.
4. Reload `/offline` and a cached public shell route.
5. Confirm private/API routes are not served from cache.

Observed with Playwright Chromium:

- `navigator.serviceWorker.controller !== null`
- cache key: `topshelf-app-shell-pwa-shell-v4`
- cached `/kitchen`: yes
- cached `/offline`: yes
- cached private/API paths: no
- offline `/offline` load: yes

Source review confirms non-GET requests return without `respondWith`, so mutation requests bypass cache.

## Mobile Viewports

- 320px: passed, `#main-content` present, no horizontal overflow.
- 375px: passed, `#main-content` present, no horizontal overflow.
- 768px: passed, `#main-content` present, no horizontal overflow.
- 1366x768: passed, `#main-content` present, no horizontal overflow.

Phone emulator e2e:

```bash
PLAYWRIGHT_BASE_URL=http://localhost:3001 pnpm --filter @topshelf/tests test:e2e:phone
```

Result: 2/2 passed on the Pixel 7 project.

## Real Device Install

Status: not run locally. Requires deployed HTTPS URL or tunnel.

## Cleanup Items

- Complete real-device install/offline check when a reachable HTTPS URL is available.
