# Tests

This directory contains repository-level test suites that are not scoped to a single package.

## Areas

- `e2e/` for end-to-end coverage.
- `integration/` for cross-package integration coverage.

## Indexing Notes

- Package-local tests stay beside each package.
- Cross-cutting test assets belong here.
