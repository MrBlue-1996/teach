# Apps

This directory contains user-facing applications.

## Current Apps

- [web](web) is the main web application.

## Indexing Notes

- Start here for UI routes, components, hooks, and client-side API calls.
- Look in `web/src/app` for route structure.
- Look in `web/src/components` for shared interface components.
